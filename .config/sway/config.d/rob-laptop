input * {
	xkb_layout "gb"
}

input type:touchpad {
    tap enabled
    natural_scroll enabled
    dwt enabled
    dwtp enabled
}

set $lock 'gtklock -d'
bindswitch --reload --locked lid:on exec $lock

exec fusuma
